This script removes vscode cache directories `/Cache`, `/CachedData`, `/CachedExtensionVSIXs`.\
Also script analyses `/User/workspaceStorage` directory and removes non-existing projects data.\
Dry run is supported.